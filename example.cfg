number = 69;

double = 4.21235;

boolean = true;

string = "pipewire & "
"fastcompmgr & "
"feh --bg-fill ~/Downloads/peng.jpg & "
"setxkbmap -layout us,ru -option grp:win_space_toggle -option ctrl:nocaps & "
"xrandr --output HDMI-1 --primary --mode 1920x1080 --rate 144 --left-of eDP-1 &";

// Single line comment

/*
 * Multiline comment
 * Line under this would cause redefinition error
 * number = 55;
 */

// Arrays must contain variables with one type
// bad_array = [1, 2.0, true]; will return error
array = [1, 2, 3, 4];

list = (true, "Some string", 25);

structure = {
    a = 5;
    b = 10;

    nested = {
        double = 144.441;
        ints = [1, 2, 3, 5, 6];
        list = (1, "Hello, world!", 4.20, false);
    };
};
